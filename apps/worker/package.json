{
  "name": "@spain/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Reserved worker — Phase 1 uses JOBS_PROVIDER=inline",
  "scripts": {
    "build": "node -e \"console.log('worker placeholder')\"",
    "lint": "node -e \"process.exit(0)\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\""
  }
}
